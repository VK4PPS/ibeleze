import { Produtos } from './produtos';

export class Item{
    quantidade : number;
    produto : Produtos;
  item: import("./../model/produtos").Produtos;
  produtos: import("./../model/produtos").Produtos;
}
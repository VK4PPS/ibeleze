import { Produtos } from './produtos';

export class Item{
    quantidade : number;
    produto : Produtos;
  item: import("src/model/produtos").Produtos;
  produtos: import("src/model/produtos").Produtos;
}